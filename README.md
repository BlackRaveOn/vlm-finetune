# 🦙 vlm_finetune — Унифицированный фреймворк для файнтюнинга Vision-Language моделей

`vlm_finetune` — это лёгкий фреймворк для **файнтюнинга Vision-Language моделей (VLM)** на пользовательских данных.  
Он предоставляет простой и единый API для загрузки, обучения и тестирования моделей вроде **LLaVA** и **Qwen-VL**.

---

## 💡 Основная идея

Фреймворк создан, чтобы вы могли:

- 🔄 Быстро адаптировать любую VLM-модель под свои данные (например, рентгенограммы, мед. отчёты, визуальные инструкции и т.д.);  
- ⚙️ Использовать единый интерфейс `AutoVlmModel` для управления всеми моделями;  
- 🧠 Использовать встроенные дефолтные параметры для LoRA и обучения;  
- 📊 Оценивать качество модели прямо после fine-tune через метрики вроде BERTScore.

---

## 🧩 Установка

```bash
git clone https://github.com/BlackRaveOn/vlm-finetune.git
cd vlm_finetune
poetry install
```

---

## 🚀 Быстрый старт

```python
from vlm_finetune import AutoVlmModel

model = AutoVlmModel.from_name(
    model_name="llava",
    model_path="llava-hf/llava-1.5-7b-hf"
)

prompt = "Какой диагноз можно поставить по рентгенограмме кошки?"
image_path = "example.jpg"

model.finetune(
    dataset_path="llava_vet_dataset_train.json",
    output_dir="llava_tuned",
    prompt=prompt,
    num_train_epochs=3
)

model_answer = model.predict(image=image_path, prompt=prompt)
print(model_answer)
```
Больше примеров в `examples`

